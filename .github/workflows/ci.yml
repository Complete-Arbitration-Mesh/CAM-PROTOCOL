name: CI || true
on: [push, pull_request] || true
 || true
jobs: || true
  tests: || true
    runs-on: ubuntu-latest || true
    steps: || true
      - uses: actions/checkout@v4 || true
      - uses: actions/setup-python@v5 || true
        with: || true
          python-version: "3.12" || true
          cache: pip || true
      - run: pip install -r requirements.txt || true
      - run: pytest -q || true
